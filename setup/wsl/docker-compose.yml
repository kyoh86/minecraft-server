services:
  world:
    image: itzg/minecraft-server:java21
    container_name: mc-world
    restart: unless-stopped
    ports:
      - "25565:25565"
    environment:
      EULA: "TRUE"
      TYPE: PAPER
      VERSION: "1.21.11"
      MEMORY: "4G"
      SERVER_NAME: "main"
      MOTD: "Main Server"
      ONLINE_MODE: "TRUE"
      ENABLE_RCON: "FALSE"
      CREATE_CONSOLE_IN_PIPE: "true"
      MODRINTH_PROJECTS: "luckperms,multiverse-core,multiverse-portals"
    volumes:
      - ./runtime/world:/data
    networks:
      - mcnet

networks:
  mcnet:
    driver: bridge
