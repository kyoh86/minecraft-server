# ClickMobs を許可するリージョンIDをワールドごとに列挙する。
# 空配列のワールドでは ClickMobs は無効。
allowed_regions:
  mainhall: []
  residence:
    - clickmobs_allowed
  resource:
    - clickmobs_allowed
  factory:
    - clickmobs_allowed

status_bossbar:
  enabled: true
  spawn_protected_region_id: spawn_protected
  spawn_protected_text: "保護エリア（建築・破壊不可）"
  spawn_protected_color: RED
  clickmobs_allowed_text: "ClickMobs許可エリア"
  clickmobs_allowed_color: GREEN
